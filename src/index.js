Game = require("./components/Game");
utils = require("./utils");

const initGame = (numPlayers, handSize, rounds, trackScore) => {};

function gameLoop() {
  let game = new Game(3, 7, 1, false);
}

gameLoop();
